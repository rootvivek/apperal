// Add these scripts to your package.json scripts section:

{
  "scripts": {
    "seed": "tsx scripts/seed.ts",
    "seed:db": "tsx scripts/seed.ts"
  }
}

// Note: You'll need to install tsx for TypeScript execution:
// npm install -D tsx
// Or use ts-node:
// npm install -D ts-node
// Then change the script to: "ts-node scripts/seed.ts"

