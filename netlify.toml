[build]
  command = "npm run build"
  # Netlify Next.js plugin will handle the publish directory automatically
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify Next.js plugin - handles routing, serverless functions, and image optimization
[[plugins]]
  package = "@netlify/plugin-nextjs"

