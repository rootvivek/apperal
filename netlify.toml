[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Enable Next.js standalone output
[next]
  standalone = true

# Handle Next.js rewrite rules
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

